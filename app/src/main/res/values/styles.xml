<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/rosaChiaro</item>
        <item name="colorPrimaryDark">@color/verdeScuro</item>
        <item name="colorAccent">@color/violaChiaro</item>
    </style>

</resources>
