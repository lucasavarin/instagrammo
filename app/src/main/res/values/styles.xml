<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/rosaChiaro</item>
        <item name="colorPrimaryDark">@color/verdeScuro</item>
        <item name="colorAccent">@color/violaChiaro</item>
    </style>

    <style name="FloatingLoginHint" parent="@android:style/TextAppearance">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/rosaChiaro</item>
    </style>

</resources>
